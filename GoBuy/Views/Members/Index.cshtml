 @model GoBuy.ViewModel.IndexViewModel

             

 @{
     Layout = "~/Views/_Shared/_LayoutPage1.cshtml";
 }

    <!DOCTYPE HTML>

    <html>
    <head>
        <meta name="viewport" content="width=device-width" />
        <title>Index</title>
    </head>
    <body>
        <p>
           
    @Html.ActionLink("Create New", "Create")
</p>
        <table class="table">
            <tr>
               
            </tr>
       
                @foreach (var m in Model.member)
                {
                           
                     <p>
                        <label>@m.Memno .</label>
                        <span>
                            <a href="Details?categoryId=@m.Memno">@m.EnName</a>
                        </span>
                    </p>
                }
        </table>


    </body>
</html>
